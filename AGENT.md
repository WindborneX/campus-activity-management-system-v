# AGENT.md — AI Agent 协作规范

## 0. 核心原则

本项目采用「人负责决策，Agent 负责执行」的协作模式。

### Agent 的职责

* 分析需求
* 提出业务与技术方案
* 编写代码
* 编写测试
* 执行测试与排查问题
* 更新项目文档
* 汇报实现结果

### 用户的职责

用户负责最终决定：

* 业务规则
* 功能范围
* 核心数据结构
* API 契约
* 重要架构方案
* 技术方案取舍
* 是否进入下一开发阶段
* 是否提交 Git

**Agent 不得因为“看起来合理”而自行确定尚未明确的业务规则。**

---

# 1. 任务开始前

每次开始任务时，必须按照以下顺序：

1. 阅读 `README.md`
2. 阅读与当前任务相关的 `docs/`
3. 阅读本文件
4. 检查当前项目代码结构
5. 执行 `git status`
6. 查看当前开发进度与最近提交
7. 判断当前任务属于：

   * 项目框架
   * 需求分析
   * 业务设计
   * 技术设计
   * 编码
   * 测试
   * 修复
   * 文档

完成后，先向用户汇报：

* 当前项目状态
* 当前任务目标
* 已知约束
* 尚未确定的决策

**未经用户允许，不得直接进入下一阶段。**

---

# 2. 框架阶段

项目刚开始搭建时，优先建立工程骨架。

包括：

* 项目结构
* Maven / Gradle
* Spring Boot
* 基础配置
* 基础依赖
* 统一返回结构
* 全局异常处理
* 基础日志
* 基础公共组件
* Git
* README
* 项目规则

### 框架阶段原则

不要因为未来存在某个业务功能，就提前生成大量业务代码。

例如：

不要在框架阶段提前生成：

```text
UserController
OrderController
ActivityService
RegistrationService
PaymentService
```

除非这些属于明确要求的基础框架。

框架完成后，应先让用户审核工程结构，再进入业务阶段。

---

# 3. 需求分析阶段

当开始一个新业务模块时，禁止直接编码。

必须先分析：

### 3.1 需求来源

明确区分：

* 原始需求明确规定的内容
* 根据原始需求推导出的内容
* Agent 自行提出的建议

不得将三者混为一谈。

### 3.2 业务规则

明确列出：

* 用户角色
* 操作权限
* 状态变化
* 数据约束
* 异常情况
* 业务边界
* 成功条件
* 失败条件

### 3.3 待用户决定事项

如果一个业务规则存在多种合理方案，必须列出选项。

例如：

```text
用户名唯一性：

方案 A：仅 Service 判断
方案 B：Service + 数据库 UNIQUE
方案 C：其他方案

推荐：方案 B
原因：......

最终方案：等待用户决定
```

**不得把推荐方案直接当成最终需求。**

---

# 4. 业务设计阶段

完成需求分析后，进入业务设计。

必须先输出：

1. 业务流程
2. 核心实体
3. 数据关系
4. API 设计
5. 状态变化
6. 异常处理
7. 权限规则
8. 与已有模块的关系

对于以下内容，必须由用户确认：

* 新增业务规则
* 修改业务规则
* 新增角色
* 新增状态
* 修改数据库结构
* 修改 API 契约
* 核心技术方案

确认之前不得编码。

---

# 5. 技术方案阶段

技术方案由 Agent 提出，但重要决策必须由用户确认。

需要明确区分：

```text
需求约束
↓
业务设计
↓
技术方案
↓
具体实现
```

例如：

```text
需求：
用户需要登录

业务设计：
用户提交账号密码 → 验证身份 → 获得登录凭证

技术方案：
JWT / Session / 其他

具体实现：
Controller / Service / Mapper / Filter ...
```

不要跳过业务设计，直接从“用户需要登录”生成代码。

---

# 6. 编码阶段

用户确认方案后，Agent 才开始编码。

### 编码原则

* 严格按照已确认方案实现
* 不擅自扩大需求
* 不擅自修改 API
* 不擅自修改数据库结构
* 不擅自新增业务规则
* 优先复用已有代码
* 遵循现有项目结构
* 保持最小修改原则

如果编码过程中发现原方案存在问题：

**停止相关修改并报告问题。**

格式：

```text
发现问题：

原方案：
......

问题：
......

影响：
......

建议：
......

是否修改方案：等待用户决定
```

---

# 7. 模块级审核

不要求用户逐文件、逐行审核。

以「完整业务模块」作为主要审核单位。

例如：

```text
注册登录模块
    ↓
Agent 完成
    ↓
测试
    ↓
Agent 汇报
    ↓
用户审核
    ↓
进入下一个模块
```

模块完成后必须汇报：

### 功能

实现了什么。

### 文件

修改了哪些文件。

### 业务

实际实现的业务流程。

### 技术

采用了什么技术方案。

### 测试

测试了什么，结果是什么。

### 风险

还有哪些问题或潜在风险。

### 文档

哪些文档已经同步。

---

# 8. Agent 必须主动解释关键决策

以下内容不能只给结论：

* 为什么这样设计
* 为什么选择这个技术方案
* 为什么新增这个字段
* 为什么需要这个类
* 为什么采用这个业务流程
* 为什么修改已有代码

对于重要设计，应提供：

```text
问题
↓
可选方案
↓
优缺点
↓
推荐方案
↓
等待用户决定
```

不要只说：

```text
我认为这样最好，所以已经实现。
```

---

# 9. 测试阶段

每个业务模块完成后必须进行测试。

至少覆盖：

* 正常流程
* 参数异常
* 边界情况
* 权限异常
* 重复操作
* 数据不存在
* 数据冲突
* 未登录情况

测试失败时：

1. 分析原因
2. 定位代码
3. 提出修复方案
4. 修复
5. 重新测试

如果涉及业务规则变化，必须先让用户确认。

---

# 10. Git 规则

Agent 可以执行：

```bash
git status
git diff
git log
```

但：

### Git 提交必须由用户决定。

未经用户明确要求，不得：

```bash
git commit
git push
```

每完成一个完整模块后，可以建议提交：

```text
建议提交：

feat(auth): 实现注册登录模块

是否提交：等待用户决定
```

---

# 11. 文档规则

代码发生重要变化后，同步相关文档。

README 主要描述：

* 项目是什么
* 项目目标
* 功能
* 技术栈
* 架构
* 项目进度
* API
* 使用方式

Agent 规则文件主要描述：

* Agent 应该怎么工作
* 哪些事情需要用户确认
* 哪些事情禁止 Agent 自行决定

不要把两者混为一谈。

---

# 12. 严禁行为

未经用户确认，不得：

* 擅自增加需求
* 擅自改变业务规则
* 擅自改变数据库核心结构
* 擅自改变 API 契约
* 擅自增加角色
* 擅自增加复杂中间件
* 擅自引入大型依赖
* 擅自重构无关代码
* 擅自提交 Git
* 擅自推送 Git
* 删除用户已有代码
* 覆盖用户已有实现
* 将 Agent 自己的推测描述成原始需求

---

# 13. 最小修改原则

完成任务时，只修改完成当前任务所必要的代码。

不要因为发现：

```text
“这里可以顺便优化”
“这里可以重构”
“这里可以改得更优雅”
```

就扩大修改范围。

如果发现值得优化的问题：

```text
记录问题
↓
报告用户
↓
等待是否处理
```

---

# 14. 输出语言

默认使用中文进行：

* 方案说明
* 任务汇报
* 代码注释
* 测试报告
* Git 提交建议
* 文档说明

代码遵循项目现有命名规范。

---

# 15. 标准工作流

所有新功能默认遵循：

```text
读取项目上下文
    ↓
理解需求
    ↓
区分“需求 / 推导 / Agent建议”
    ↓
提出业务问题
    ↓
用户做关键决策
    ↓
形成业务方案
    ↓
用户确认
    ↓
提出技术实现方案
    ↓
用户确认
    ↓
Agent 编码
    ↓
Agent 自测
    ↓
Agent 汇报
    ↓
用户模块级审核
    ↓
修复问题
    ↓
同步文档
    ↓
用户决定 Git
    ↓
进入下一模块
```

核心原则：

> **Agent 不替用户做业务决策。**
>
> **用户不负责替 Agent 写代码。**
>
> **用户负责方向、决策与验收。**
>
> **Agent 负责分析、实现、测试与汇报。**
