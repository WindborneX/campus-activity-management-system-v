# AGENT.md — AI Agent 行为规范

> 本文档规范 AI Agent 在本项目的全部行为。项目上下文（工程意图、需求、架构、进度）统一维护在 [README.md](README.md) 中，Agent 每次会话必须先读取。

## 0. 启动检查（每次会话必做，未完成前不得修改代码）

开始任何任务前，必须按序完成：

1. 确认项目根目录；
2. 读取 [AGENT.md](AGENT.md)（本文档）；
3. 读取 [README.md](README.md)，重点确认：工程意图（§1）、业务规则（§2.3）；
4. 检查 README.md §6 当前开发状态，判断当前所处阶段，从该阶段继续，不重做已完成工作；
5. 根据当前任务定位相关源码；
6. 不确定时向用户提问，禁止凭空假设需求。

完成启动检查后，先向用户报告以下内容，再开始工作：

- 当前阶段（对应 README §6 哪一项）；
- 已读取的文档；
- 对任务的理解；
- 下一步计划方案（由用户批改确认后方可开始编码）。

## 1. 开发工作流（模块制）

### 1.1 开发前：方案先行与用户确认

每次开发一个新模块（功能点/接口/页面）前，必须先向用户提交实现方案：涉及文件清单、接口或页面设计、关键实现要点、验证方式。方案由用户批改确认后方可开始编码。

**以下情况必须等待用户确认后才能实施：**

- 新模块；
- 架构调整；
- 数据库结构调整；
- 新增依赖；
- 修改既有业务规则；
- 超出 README 定义范围的功能。

**以下情况可直接处理，事后报告即可：**

- 简单的 bug 修复；
- 格式调整；
- 明显错误的修复。

### 1.2 开发前检查

开始任务前必须检查：

- `git status`；
- 当前分支；
- 最近提交（`git log`）；
- README.md §6 当前开发状态；
- 是否存在未提交修改。

**不得覆盖用户已有的未提交修改。**

### 1.3 开发后：总结与提交决策
1. 每个模块完成后，向用户总结本模块完成的业务：实现的功能点（F 编号）、覆盖的业务规则（R 编号）、自测/验证情况；
2. 是否提交 git 由用户决定；未获用户同意，不得执行 git commit / push；
3. **交互留痕**：与用户交互的重要内容（提交过的方案、用户批改意见、关键决策、验证结论、提交记录）必须在完成后同步写入 README.md「8. AI 辅助开发记录」章节，保证需求 ↔ 设计 ↔ AI 交互 ↔ 代码 ↔ Git 可追踪。

## 2. 最小修改原则

优先选择最小可行修改：

- 能修改现有代码，不新建重复代码；
- 能复用现有工具，不重新实现；
- 能局部修改，不进行无关重构；
- 不为了“以后可能用到”提前设计；
- 不为了体现技术水平增加复杂架构。

## 3. 行为红线

1. **范围约束**：只实现 README.md §2 定义的 9 个功能点（F1~F9）。禁止擅自增加功能（管理员、评论、签到、通知、搜索等）；
2. **AI 产出定位**：Agent 生成的一切代码/文档均为候选成果，须经用户确认或验证通过后才算正式成果；重要决策（超出 README 已定范围的一切改动）必须先征求用户同意；
3. **变更回溯**：开发中发现原有需求/设计有问题时，先向用户报告并回到工程意图层面讨论，禁止直接改代码扩大范围；
4. **文档同步**：完成一项工作后立即勾选 README.md §6 状态清单；需求/设计/接口如有变更，同步更新 README.md 对应章节，并在会话中说明变更原因；
5. **安全红线**：禁止将密码、Token、密钥、真实个人信息写入代码或提交到 Git；敏感配置一律放本地配置文件（如 `application-local.yml`）并确保在 .gitignore 中。

## 4. 技术约束

- 技术栈固定为 README.md §3.2 所列，不得擅自更换框架或引入新依赖；确有必要时先说明理由并征得同意；
- 业务规则 R1~R7 必须在 Service 层实现，Controller 只做参数校验与转发；
- 统一响应结构 `Result<T>`，异常统一由全局异常处理器处理；
- 前后端接口以 README.md §3.4 API 清单为准，新增/修改接口须同步文档。

## 5. Git 行为规范

- 小步提交，按 README.md §5 的提交序列推进，禁止一次性提交全部代码；
- 每次提交前须按 §1.3 向用户总结完成内容，由用户决定是否提交；未经用户同意不得 commit；
- 提交信息格式：`type(scope): 说明`（type ∈ init/docs/feat/fix/test），说明须反映本次修改内容；
- 仅在用户明确要求时才 push 到远程 `origin`；
- 提交前检查不含敏感信息与无关文件（依赖目录、构建产物、IDE 配置）。

## 6. 输出要求

- 回复与代码注释使用中文；
- 代码保持简洁，不加超出当前任务的多余功能、注释和抽象；
- 涉及代码位置时给出可点击的文件链接。

## 7. Agent 禁止事项速查

| 禁止 | 原因 |
|---|---|
| 未经用户批改实现方案就开始编码 | 违背 §1.1 方案先行 |
| 跳过启动检查 / 开发前检查 | 违背 §0、§1.2 |
| 覆盖用户已有的未提交修改 | 违背 §1.2 |
| 模块完成未总结业务、或擅自 commit | 违背 §1.3 提交决策权在用户 |
| 直接生成整套系统 | 违背实验要求（需求先行、逐步提交） |
| 擅自扩大功能范围 | 违背工程意图约束 |
| 未经确认重写已验证代码 | 破坏可追踪性 |
| 跳过文档同步 | 破坏需求↔设计↔代码↔Git 的可追踪关系 |
| 提交敏感信息 | 信息安全红线 |
