# AGENT.md — AI Agent 协作规范（精简版）

## 0. 核心原则

> **人负责业务决策与审批，Agent 负责分析、实现与验证。**

* Agent 不得自行确定业务规则、扩大功能范围或引入未经批准的技术（Redis/MQ/微服务/管理员等一律默认不做）。
* V1.0 业务边界：角色仅 STUDENT/TEACHER；范围仅 README 定义的 F1~F9 / R1~R7。
* 默认中文输出；最小修改；禁止提交密码/Token/数据库真实密码。

## 1. 每个模块的标准流程

```text
提出模块
↓
Agent 分析现有代码
↓
Agent 给出：
  ① 核心业务流程,一步一步来写
  ② 关键业务规则
  ③ 实现方案
  ④ 需要注意的问题（最多 3 个）
↓
人审批
↓
Agent 实现
```

## 2. 审批包格式（一次给全，不挤牙膏）

```text
【等待审批】<模块名>（对应 F/R 编号）

① 核心业务流程：<正常流程步骤 + 失败时如何处理>
② 关键业务规则：<逐条编号，含操作权限（谁可操作、前提）与数据约束>
③ 实现方案：<技术选型及为什么、本次修改哪些文件、不修改哪些>
④ 需要注意的问题：<最多 3 个：并发/越权/重复操作/状态错误等风险及建议>

需要人工确认：<1. 2. 3.>
```

审批通过前不写代码。实现中发现必须扩大修改范围：暂停并说明，不静默扩大。

## 3. 实现与验证

* 严格按批准方案实现；复用现有代码；不重构无关代码。
* 验证必须覆盖：正常流程、边界（重复/不存在/已满/非法参数）、权限（未登录/角色越权/跨用户数据）。
* 完成后简短汇报：做了什么 → 测试结果（通过/失败清单）→ 剩余风险。

## 4. Git

* 一个业务闭环一个提交；commit / push 必须由用户明确决定，Agent 只能建议。
* 禁止：改历史、强推、大杂烩提交、提交敏感信息。

## 5. AI 使用记录

* 每个关键步骤（方案、批改意见、决策、验证结论、提交）经用户确认后**必须立即写入** README.md「8. AI 辅助开发记录」；**未写入前该确认不算完成**。
* 重点记录「Agent 提出了什么，人为什么接受、修改或拒绝」。

## 6. 完成标准（缺一不可）

```text
□ 方案经人审批    □ 代码实现并编译通过    □ 正常/边界/权限测试通过
□ 风险已说明      □ README §8 已留痕      □ Git 提交经用户同意
□ 人能解释该功能的业务逻辑
```
