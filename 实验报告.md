# 软件工程实验一 · 基于工程意图的软件迭代开发

## 实验报告

**项目名称**：校园活动管理系统 V1.0
**实验名称**：软件工程实验一——基于工程意图的软件迭代开发
**技术栈**：Spring Boot 3.3.5 + MyBatis-Plus + MySQL 9.6（后端）／ Vue3 + Vite + Element Plus（前端）／ Nginx（同源联调）
**代码仓库**：https://github.com/WindborneX/campus-activity-management-system-v
**协作模式**：人负责业务决策与审批，Agent 负责分析、设计、实现与验证

---

## 一、实验目的

1. 理解「工程意图」对软件迭代开发的驱动作用——以最小可用业务闭环为目标，而非堆砌功能。
2. 掌握需求分析、业务设计、技术方案、编码实现、验证测试的完整迭代流程。
3. 学会在 AI 辅助开发中保持人的主导地位：AI 提出方案，人做关键决策，需求 ↔ 设计 ↔ 代码 ↔ Git 可追踪。
4. 通过实际项目（校园活动管理系统 V1.0）跑通「教师发布 → 学生报名 → 教师查看名单」的业务闭环。

---

## 二、需求分析与工程意图

### 2.1 工程意图

学校活动组织目前依靠线下通知，学生获取信息分散、报名靠人工统计。V1.0 的目标不是做一个功能齐全的平台，而是用最小可用系统跑通核心业务闭环：

> **教师线上发布活动 → 学生线上浏览与报名 → 教师查看报名名单。**

本轮范围严格控制在：注册登录、活动发布与管理、活动浏览与报名，共 9 个功能点。不引入管理员、评论、签到、推荐等扩展功能。

### 2.2 角色与权限边界

| 角色 | 允许的操作 |
|---|---|
| STUDENT 学生 | 注册、登录、浏览活动、查看详情、报名、取消报名、查看我的报名 |
| TEACHER 教师 | 登录、发布活动、修改/取消自己的活动、查看自己发布的活动、查看活动报名名单 |

### 2.3 功能需求（F1 ~ F9）

| 编号 | 功能 | 角色 |
|---|---|---|
| F1 | 用户注册（用户名 + 密码 + 身份） | 学生 / 教师 |
| F2 | 用户登录（签发 JWT） | 学生 / 教师 |
| F3 | 发布活动（标题、简介、地点、时间、报名截止、人数上限） | 教师 |
| F4 | 管理活动（查看自己的活动、修改、取消） | 教师 |
| F5 | 浏览活动列表（按阶段筛选） | 公开 |
| F6 | 查看活动详情 | 公开 |
| F7 | 报名活动 | 学生 |
| F8 | 取消报名 | 学生 |
| F9 | 查看活动报名名单 | 教师（仅发布者） |

### 2.4 业务规则（R1 ~ R7）—— 系统的「对错标准」

| 编号 | 规则 |
|---|---|
| R1 | 未登录用户只能浏览，报名/发布/管理操作必须登录（401） |
| R2 | 同一学生对同一活动只能报名一次（Service 判断 + 数据库唯一约束双重保障） |
| R3 | 已报名人数 ≥ 人数上限时拒绝报名 |
| R4 | 当前时间 ≥ 报名截止时间时拒绝报名；截止时间必须严格早于活动开始时间 |
| R5 | 只有活动发布者本人能修改/取消该活动、查看其报名名单（跨教师 403） |
| R6 | 活动被取消后不可再报名、不可再修改（软删除，保留报名记录） |
| R7 | 密码使用 BCrypt 单向哈希存储，任何接口不返回密码字段 |

### 2.5 验收依据（工程意图的完成标准）

1. 教师发布一个容量为 2 人的活动；
2. 3 名学生依次报名，第 3 人因满员被拒；
3. 学生可取消报名，取消后空出名额可再报；
4. 教师可查看报名名单；活动取消后不可再报名。

---

## 三、软件设计

### 3.1 总体架构

```
浏览器
  ↓ 同源访问 http://localhost:8090
Nginx（统一入口，剥离 Origin 防后端跨域误判）
  ├─ /api/* → 后端 Spring Boot :8080
  └─ 其他   → 前端 Vite :5173（开发期）
后端 Spring Boot 3
  ├─ Controller 层（参数绑定，无业务逻辑）
  ├─ Service 层（全部业务规则 R1~R7 集中于此）
  ├─ Mapper 层（MyBatis-Plus）
  └─ JWT 拦截器（认证：你是谁）
MySQL 9.6（3 张表）
```

### 3.2 技术选型与理由

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Spring Boot 3.3.5 | 成熟、生态全、单模块即可满足 V1.0 规模 |
| ORM | MyBatis-Plus 3.5.7 | 单表 CRUD 快速，减少样板代码 |
| 鉴权 | JWT（jjwt）+ HandlerInterceptor | 无状态、依赖少；认证与授权分离，拦截器只管「你是谁」，Service 管「你能不能做」 |
| 密码加密 | spring-security-crypto 的 BCrypt | 仅引入加密模块，不引入完整 Spring Security（最小依赖） |
| 前端 | Vue3 + Vite 5 + Element Plus | 熟悉栈、组件现成、学院风定制成本低 |
| 状态管理 | Pinia 2 | Vue3 官方推荐，轻量 |
| HTTP | Axios | 统一拦截器处理 Result 解包与 401 跳转 |
| 联调 | Nginx（Docker） | 浏览器只访问同源 Nginx 端口，不直连后端，避免跨域 |
| 数据库 | MySQL 9.6 | 常规选择 |

**明确不引入的技术**（规范红线）：Redis、消息队列、微服务、分布式锁、Docker 编排、第三方登录、管理员系统等——V1.0 规模下属于过度设计。

### 3.3 数据库设计（3 张表）

```sql
-- user 用户
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) NOT NULL UNIQUE,
  password_hash VARCHAR(100) NOT NULL,
  role VARCHAR(10) NOT NULL COMMENT 'STUDENT/TEACHER',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- activity 活动
CREATE TABLE activity (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  teacher_id BIGINT NOT NULL,
  title VARCHAR(100) NOT NULL,
  description TEXT,
  location VARCHAR(100),
  start_time DATETIME NOT NULL,
  end_time DATETIME NOT NULL,
  signup_deadline DATETIME NOT NULL,
  max_participants INT NOT NULL,
  status VARCHAR(10) NOT NULL DEFAULT 'ACTIVE',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- registration 报名
CREATE TABLE registration (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  activity_id BIGINT NOT NULL,
  student_id BIGINT NOT NULL,
  registered_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_activity_student (activity_id, student_id)
);
```

设计原则：**Service 层负责正常业务判断与友好提示，数据库约束负责最终数据完整性**，二者不可互相替代（如报名唯一性：Service 先查 + UNIQUE 兜底）。

### 3.4 API 设计（11 个接口）

| 方法 | 路径 | 功能 | 权限 |
|---|---|---|---|
| POST | /api/auth/register | 注册 | 公开 |
| POST | /api/auth/login | 登录 | 公开 |
| GET | /api/activities?stage= | 活动列表 | 公开 |
| GET | /api/activities/{id} | 活动详情 | 公开 |
| POST | /api/activities | 发布活动 | 教师 |
| PUT | /api/activities/{id} | 修改活动 | 发布教师本人 (R5) |
| DELETE | /api/activities/{id} | 取消活动 | 发布教师本人 (R5) |
| GET | /api/activities/mine | 我的活动 | 教师 |
| POST | /api/activities/{id}/signup | 报名 | 学生 (R2/R3/R4/R6) |
| DELETE | /api/activities/{id}/signup | 取消报名 | 学生本人 |
| GET | /api/activities/{id}/registrations | 报名名单 | 发布教师本人 (R5) |
| GET | /api/registrations/mine | 我的报名 | 学生 |

统一响应格式：`Result<T> { code, message, data }`，HTTP 状态码与业务码同步。

### 3.5 前端页面规划

| 页面 | 路由 | 说明 |
|---|---|---|
| 登录/注册 | /login | 双 Tab，与后端一致的前端校验 |
| 活动列表 | /activities | 4 Tab 阶段筛选 + 卡片 + 报名进度条 |
| 活动详情 | /activities/:id | 信息卡 + 报名/取消按钮矩阵 |
| 我的报名 | /my-registrations | 表格 + 行内取消 |
| 活动管理 | /manage | 教师：列表 + 创建/编辑弹窗 + 名单弹窗 |

---

## 四、迭代开发过程

### 4.1 Git 提交历史（逐步形成，共 12 个提交）

```
f5da139 feat(web): 教师活动管理页（发布/编辑/取消/名单）
c40be5b feat(web): 活动列表详情页与学生报名交互（含我的报名）
33e9182 feat(web): 前端工程骨架与登录注册页（Nginx 同源联调）
45e4061 docs: 新增业务需求与功能逻辑说明 HTML
445e619 feat(registration): 活动浏览与报名接口（R1~R6 业务规则与权限校验）
4d1d313 feat(activity): 教师活动发布与管理接口（角色与所有权校验）
ce04724 feat(auth): 实现注册登录接口（BCrypt 加密与 JWT 签发）
07e90ec docs: 精简 Agent 协作规范为模块审批流程制
4ad0ee6 docs: 重构 AI Agent 协作规范（人决策/Agent 执行模式）
f7b353d docs: 建立 AI 辅助开发记录并固化协作留痕规则
2f84d6a feat(backend): 数据库建表脚本与 Spring Boot 项目骨架
d7076fb init: 建立仓库与项目文档（工程意图、需求分析、架构设计、Agent 规范）
```

每个提交对应一个明确的业务闭环，符合「一个需求 → 一个业务闭环 → 一次验证 → 一个 Git Commit」的原则。

### 4.2 协作流程（人决策 + Agent 执行）

本项目采用「人负责业务决策与审批，Agent 负责分析、设计、实现与验证」的协作模式。每个新模块遵循以下标准流程：

```
提出模块
  ↓
Agent 分析现有代码
  ↓
Agent 给出：① 核心业务流程 ② 关键业务规则 ③ 实现方案 ④ 需注意的问题（≤3）
  ↓
人审批（确认业务规则、技术方案、待决策点）
  ↓
Agent 实现
  ↓
Agent 测试 + 汇报（功能/文件/业务/技术/测试/风险/文档）
  ↓
人审核 → 决定是否提交 Git
```

关键规则：
- Agent 不得因「看起来合理」自行确定尚未明确的业务规则；
- 每个关键决策经用户确认后立即写入 README §8「AI 辅助开发记录」；
- 涉及业务规则、数据库结构、API 契约、新增依赖的变更必须等人工审批；
- 简单 bug 修复、格式调整可直接处理，事后报告。

### 4.3 关键决策记录

| 决策点 | Agent 提出的方案 | 人的决策 | 原因 |
|---|---|---|---|
| 旧库冲突 | 方案 A 备份后重建 / B 直接重建 / C 适配旧表 | A 备份后重建 | 保留旧数据防万一 |
| 活动模块规则 | ① 截止≤开始 ② 容量不得小于已报名 ③ 并发处理 | ① 严格 < ② 采纳 ③ 不考虑并发 | V1.0 不堆技术 |
| 报名模块 | 新增「我的报名」API / 取消时间限制 / 列表阶段语义 | 新增 API / 活动开始前可取消 / 用阶段筛选 | 前端需要 |
| 前端骨架 | 依赖最小集 / 模块切分 / 目录名 / 联调方式 / 视觉 | 照单安装 / 合并模块 / web / Nginx / 简洁学院风 | 最小可用 |
| Nginx 获取 | 本机路径 / 下载便携版 / 稍后自装 / Docker | Docker | 镜像已存在 |
| 教师管理页 | 时间格式 / 弹窗共用 / 名单分页 | 原生 Date / 共用 / 不分页 | 不增依赖 |

---

## 五、实现与验证

### 5.1 后端验证

后端 11 个接口全部通过端到端接口测试（curl + 浏览器自动化）：

**认证模块（A 系列）**

| 场景 | 期望 | 结果 |
|---|---|---|
| 学生/教师注册 | 200，返回用户信息 | ✅ |
| 重复用户名注册 | 400「用户名已存在」 | ✅ |
| 密码过短 / 非法角色 | 400 对应提示 | ✅ |
| 正确密码登录 | 200，返回 JWT | ✅ |
| 错误密码 / 不存在用户 | 统一「用户名或密码错误」（防探测） | ✅ |
| 无 token 访问受保护接口 | 401 | ✅ |

**活动管理模块（B 系列）**

| 场景 | 期望 | 结果 |
|---|---|---|
| 教师创建活动 | 200 | ✅ |
| 学生创建活动 | 403「仅教师可执行该操作」 | ✅ |
| 截止=开始 / 开始在过去 / 结束<开始 | 400 | ✅ |
| 跨教师修改（R5） | 403 | ✅ |
| 本人修改 | 200 | ✅ |
| 取消活动 | 200（软删除） | ✅ |
| 修改已取消活动 | 400 | ✅ |
| 容量改小于已报名人数 | 400 | ✅ |

**报名模块（C 系列）**

| 场景 | 期望 | 结果 |
|---|---|---|
| 公开列表/详情 | 200，含 currentCount、stage | ✅ |
| 教师报名 | 403 | ✅ |
| 学生报名成功 | 200 | ✅ |
| 重复报名（R2） | 400 | ✅ |
| 满员（R3） | 400「报名人数已满」 | ✅ |
| 过截止时间（R4） | 400「报名已截止」 | ✅ |
| 已取消活动报名（R6） | 400「活动已取消，无法报名」 | ✅ |
| 取消报名 | 200 | ✅ |
| 重复取消 | 400 | ✅ |
| 名单：发布者/他人/学生 | 200/403/403 | ✅ |
| 我的报名：学生/教师/未登录 | 200/403/401 | ✅ |
| 未登录报名 | 401 | ✅ |

### 5.2 前端验证（浏览器自动化）

**登录注册页**：登录页渲染、错误密码提示、正确登录跳转+角色导航、学生访问教师页被守卫拦回、退出回登录页——全部通过，控制台零报错。

**活动列表/详情 + 报名**：列表 Tab 切换、详情信息、报名成功（按钮变取消、人数+1）、取消报名二次确认（恢复、人数-1）、我的报名列表、已截止/已满按钮禁用并显示原因——全部通过。

**教师管理页**：列表渲染、7 字段创建弹窗、已取消行按钮禁用、名单弹窗——全部通过；API 层用前端时间格式 `yyyy-MM-dd HH:mm:ss` 创建/编辑/名单/取消全部 200。

### 5.3 修复记录

| 问题 | 原因 | 修复 |
|---|---|---|
| 后端骨架：鉴权排除不分 HTTP 方法 | 拦截器 excludePathPatterns 按路径匹配，POST 公开路径被误放行 | 改为按 HTTP 方法 + 路径精确匹配 |
| 后端骨架：404 误报 500 | 全局异常处理把 NoHandlerFoundException 当兜底异常 | 单独处理，返回 404 |
| 后端骨架：异常响应 HTTP 状态码恒为 200 | 只设了业务 code 没设 HTTP 状态 | 统一 HTTP 状态码与业务码 |
| Nginx 同源后浏览器登录 403 | 浏览器 Origin:8090 被后端按跨域判定（CORS 仅白名单 5173） | Nginx 转发 /api 时剥离 Origin 头 |
| 禁用按钮点击穿透产生未捕获 Promise | 禁用态按钮被点击穿透触发请求，async reject 未捕获 | doSignup 加入口守卫 + try/catch |
| 已取消活动编辑按钮未禁用 | UX 不一致（后端虽兜底） | 前端同步 disabled |

---

## 六、工程意图达成情况

| 验收依据 | 对应功能/规则 | 状态 |
|---|---|---|
| 教师发布容量 2 人的活动 | F3 | ✅ 已实现并验证 |
| 3 名学生报名，第 3 人被拒 | F7、R3 | ✅ 已实现并验证 |
| 学生取消报名后名额释放可再报 | F8、R3 | ✅ 已实现并验证 |
| 教师查看报名名单 | F9、R5 | ✅ 已实现并验证 |
| 活动取消后不可再报名 | R6 | ✅ 已实现并验证 |

工程意图的核心业务闭环已全部跑通。

---

## 七、AI 辅助开发总结

### 7.1 AI 的角色与边界

在本项目中，AI（Agent）的角色是**分析、设计、实现、验证的执行者**，而非决策者。所有业务规则、功能范围、数据库结构、API 契约、技术方案取舍均由人最终决定。

具体体现：
- 每个新模块开发前，Agent 先输出「业务流程 + 关键规则 + 实现方案 + 待决策问题」，等人审批后才编码；
- Agent 发现的业务规则冲突、数据不一致、并发风险等，会主动提出方案但不自行实施；
- 每个关键决策经人确认后立即写入 README §8，形成需求 ↔ 设计 ↔ AI 交互 ↔ 代码 ↔ Git 的完整证据链。

### 7.2 关键交互示例

**示例 1：活动模块规则审批**

Agent 提出 3 个待决策问题：① 报名截止与开始时间关系 ② 修改容量约束 ③ 并发处理。人决策：严格小于、采纳、不考虑。Agent 据此实现。

**示例 2：报名模块契约缺口**

Agent 发现前端「我的报名」页面对应的 API 不在已批准的 10 个接口中，主动提出新增 `GET /api/registrations/mine`，经人批准后才实现，并同步更新 README API 清单。

**示例 3：Nginx 联调方式**

Agent 发现本机未装 Nginx，提出 4 种获取方案，人选择 Docker，Agent 据此创建 Docker 版 Nginx 配置并启动容器联调。

### 7.3 经验与教训

1. **业务逻辑先于代码**：Agent 必须先回答「谁可以操作、前提是什么、过程规则、成功结果、失败处理」，再写代码，避免「从需求直接跳到 Controller」。
2. **方案必须说明「为什么」**：技术方案要列可选方案 A/B 的优缺点 + 推荐原因，推荐 ≠ 最终。
3. **数据库双保险原则**：Service 校验与数据库约束不能互相替代，如报名唯一性用 Service 判断 + UNIQUE 约束。
4. **认证与授权分离**：JWT 拦截器只管「你是谁」，Service 管「你能不能做」（角色 + 资源所有权）。
5. **最小修改原则**：不为未来可能用到而提前设计，不堆技术；如 V1.0 不引入 Redis/MQ/分布式锁。

---

## 八、附录

### 8.1 项目目录结构

```
camus-activity-management-system/
├── AGENT.md                      # Agent 协作规范
├── README.md                     # 项目上下文（需求/设计/进度/AI 记录）
├── 业务需求说明.html              # 业务需求与功能逻辑可视化
├── sql/init.sql                  # 数据库建库建表脚本
├── deploy/nginx/
│   ├── nginx.conf                # 本机版 Nginx 配置
│   └── nginx.docker.conf         # Docker 版 Nginx 配置
├── server/                       # Spring Boot 后端
│   ├── pom.xml
│   └── src/main/java/com/campus/activity/
│       ├── CampusActivityApplication.java
│       ├── common/               # Result、异常处理、JwtUtil
│       ├── config/               # WebConfig（CORS + 拦截器）
│       ├── controller/           # Auth、Activity、Registration
│       ├── service/              # AuthService、ActivityService、RegistrationService
│       ├── mapper/               # MyBatis-Plus Mapper
│       ├── entity/               # User、Activity、Registration
│       └── dto/                  # 请求/响应 DTO
└── web/                          # Vue3 前端
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── main.js、App.vue
        ├── router/index.js
        ├── stores/               # auth、myReg
        ├── api/                  # request、auth、activities、registrations
        ├── layouts/MainLayout.vue
        ├── styles/main.css
        └── views/                # Login、ActivityList、ActivityDetail、MyRegistrations、Manage
```

### 8.2 启动方式

```bash
# 1. 数据库（需先配置 application-local.yml 的 MySQL 密码）
mysql -u root -p < sql/init.sql

# 2. 后端（端口 8080）
cd server && mvn spring-boot:run

# 3. 前端（端口 5173）
cd web && npm install && npm run dev

# 4. Nginx（Docker 版，浏览器访问 http://localhost:8090）
docker run -d --name campus-nginx -p 8090:80 \
  --mount type=bind,source=<项目路径>/deploy/nginx/nginx.docker.conf,target=/etc/nginx/nginx.conf,readonly \
  nginx:latest
```

### 8.3 验证账号

- 学生：`webstu / 123456`
- 教师：`tea01 / 123456`
